{
    "name": "ai-prompt-composer-api",
    "version": "0.0.0",
    "scripts": {
      "prestart": "npm run generate:api",
      "prebuild": "npm run generate:api",
      "generate:api": "openapi-generator-cli generate -i ./../openapi.yaml -g python-flask  -o ./flask/ --package-name AiPromptComposerApi"
    },
    "private": true,
    "dependencies": {
      
    },
    "devDependencies": {
    }
}